{
  "name": "@openarm/proto",
  "private": true,
  "scripts": {
    "update": "bunx buf dep update",
    "gen": "bunx buf generate",
    "build": "bun run update && bun run gen"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.59.0"
  }
}
