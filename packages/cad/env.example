# =============================================================================
# Onshape API Configuration
# =============================================================================
# Required for downloading CAD models from Onshape
# Get your API keys from: https://cad.onshape.com/appstore/dev-portal
#
ONSHAPE_API=https://cad.onshape.com
ONSHAPE_ACCESS_KEY=your_onshape_access_key
ONSHAPE_SECRET_KEY=your_onshape_secret_key

# =============================================================================
# Cloudflare R2 Configuration (for CDN deployment)
# =============================================================================
# Required for uploading processed assets to Cloudflare R2 CDN
# Get credentials from: https://dash.cloudflare.com → R2 → Manage R2 API Tokens
#
# R2 Account ID (found in R2 dashboard)
R2_ACCOUNT_ID=your_r2_account_id

# R2 API Token credentials
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# R2 Bucket name (create bucket in R2 dashboard first)
R2_BUCKET_NAME=openarm-cad-assets

# Public URL for your R2 bucket (custom domain or R2.dev subdomain)
# Examples:
#   - Custom domain: https://assets.openarm.dev
#   - R2.dev subdomain: https://pub-[random-id].r2.dev
R2_PUBLIC_URL=https://assets.openarm.dev

# =============================================================================
# Processing Configuration (optional overrides)
# =============================================================================
# These have defaults in config.json, only set to override
#
# CDN_ENABLED=true
# CDN_OUTPUT_DIR=processed
# CDN_HASH_LENGTH=12

# =============================================================================
# Development Notes
# =============================================================================
# 1. Copy this file to .env (gitignored)
# 2. Fill in your credentials
# 3. Never commit .env file to version control
#
# Quick setup:
#   cp env.example .env
#   # Edit .env with your credentials
#
# Verify configuration:
#   uv run onshape-to-robot .  # Will show errors if invalid